name: Deploy Python project to Azure Function App - AIHubApis

on:
  push:
    branches: ["master"]

env:
  AZURE_FUNCTIONAPP_NAME: 'AIHubApis'
  AZURE_FUNCTIONAPP_PACKAGE_PATH: 'AIHubBackend'
  PYTHON_VERSION: '3.11'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: dev
    steps:
    - name: 'Checkout GitHub Action'
      uses: actions/checkout@v4

    - name: 'Login via Azure CLI'
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_RBAC_CREDENTIALS_FUNCTIONAPP }}

    - name: 'Ensure Python runtime on Function App'
      run: |
        az functionapp config appsettings set \
          --subscription "AIDataSense HUb" \
          --resource-group AIHub \
          --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
          --settings FUNCTIONS_WORKER_RUNTIME=python PYTHON_VERSION=3.11

    - name: Setup Python ${{ env.PYTHON_VERSION }} Environment
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: 'Install dependencies'
      shell: bash
      run: |
        pushd './${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}'
        python -m pip install --upgrade pip
        pip install -r requirements.txt --target=".python_packages/lib/site-packages"
        popd

    - name: 'Run Azure Functions Action'
      uses: Azure/functions-action@v1
      with:
        app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
        package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
